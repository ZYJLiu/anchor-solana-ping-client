import * as anchor from "@project-serum/anchor"
import idl from "./idl.json"

async function main() {}

main()
  .then(() => {
    console.log("Finished successfully")
  })
  .catch((error) => {
    console.error(error)
  })
